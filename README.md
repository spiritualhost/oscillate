# Oscillate

Open file on timer, then close, on repeat.

## Instructions

1) Run the script in an elevated Powershell Window. Either watch or minimize, but don't close the window.

## Configuration

To edit the time the program runs for and the time the program waits to launch again, edit the values in [the config file](.\config.ps1). Values there are measured in seconds.
